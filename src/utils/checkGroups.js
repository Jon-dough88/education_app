export const handleKeyPress = values => {
    
    if(values.groupName){
        console.log(`The group name is: ${values.groupName}`)
    }
    // this.props.checkForGroup(this.props.userId, searchValue);

    
}